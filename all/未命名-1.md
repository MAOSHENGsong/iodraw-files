```mermaid
graph TB
    %% 核心标题：双边弧形，加粗强调
    Main([基于人工设计特征的<br/>表面缺陷检测算法体系]) 
    
    %% 第一层级：四大分支 (直角矩形，深色边框)
    Statistical[统计方法<br/>Statistical Methods]
    Spectral[频谱方法<br/>Spectral Methods]
    Model[模型法<br/>Model-based Methods]
    Machine[浅层机器学习<br/>Shallow Machine Learning]

    %% 第二层级：典型算法 (圆角矩形，细边框)
    %% 通过在文字中手动加入 <br/> 确保不越位
    Sub_Stat(灰度共生矩阵 GLCM<br/>局部二值模式 LBP<br/>直方图统计特征)
    Sub_Spec(傅里叶变换 FFT<br/>小波变换 WT<br/>Gabor 滤波器)
    Sub_Mod(自回归模型 AR<br/>马尔可夫随机场 MRF<br/>分形维数模型)
    Sub_Mach(支持向量机 SVM<br/>K-means 聚类<br/>人工神经网络 ANN)

    %% 建立连接
    Main --> Statistical
    Main --> Spectral
    Main --> Model
    Main --> Machine

    Statistical --- Sub_Stat
    Spectral --- Sub_Spec
    Model --- Sub_Mod
    Machine --- Sub_Mach

    %% --- 样式设计：解决对齐与越位问题 ---
    
    %% 样式1：标题样式
    style Main fill:#f2f2f2,stroke:#000,stroke-width:2px,font-weight:bold,width:260px
    
    %% 样式2：分类层级 (锁定 210px 宽度，确保容纳英文)
    classDef Level2 fill:#ffffff,stroke:#333,stroke-width:2px,width:210px,font-weight:bold,font-size:13px;
    class Statistical,Spectral,Model,Machine Level2;
    
    %% 样式3：算法层级 (宽度与 Level2 严格一致)
    classDef Level3 fill:#ffffff,stroke:#666,stroke-width:1px,width:210px,font-size:12px;
    class Sub_Stat,Sub_Spec,Sub_Mod,Sub_Mach Level3;

    %% 强制所有节点文字居中，防止视觉偏移
    style Sub_Stat text-align:center
    style Sub_Spec text-align:center
    style Sub_Mod text-align:center
    style Sub_Mach text-align:center
```