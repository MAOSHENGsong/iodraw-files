```mermaid
graph TD
    %% 核心目标：位于最顶端
    Goal([目标：表面缺陷检测<br/>Surface Defect Detection])

    %% 顶层：抽象层 (Abstraction Layer)
    Abstraction[<fontsize='14px'><b>抽象 (Abstraction)</b></fontsize><br/>模式分类与边界重塑<br/>Pattern Classification & Boundary Reshaping]

    %% 中层：提取层 (Extraction Layer)
    Extraction[<fontsize='14px'><b>提取 (Extraction)</b></fontsize><br/>特征表达与增强<br/>Feature Expression & Enhancement]

    %% 底层：去除层 (Removal Layer)
    Removal[<fontsize='14px'><b>去除 (Removal)</b></fontsize><br/>噪声抑制与净化<br/>Noise Suppression & Purification]

    %% 建立逻辑流：从底向上
    Removal ==> Extraction ==> Abstraction ==> Goal

    %% --- 逻辑与具体方法的映射 (使用虚线和二级节点) ---
    %% 映射到去除层
    style Removal fill:#e1f5fe,stroke:#01579b,stroke-width:2px;
    Sub_Rem(空间域滤波 / 频域屏蔽<br/>形态学开闭运算)
    Removal -.- Sub_Rem

    %% 映射到提取层
    style Extraction fill:#fff9c4,stroke:#fbc02d,stroke-width:2px;
    Sub_Ext(GLCM 二阶统计量<br/>Gabor 空频响应 / 形态学梯度)
    Extraction -.- Sub_Ext

    %% 映射到抽象层
    style Abstraction fill:#e1bee7,stroke:#8e24aa,stroke-width:2px;
    Sub_Abs(SVM 超平面映射<br/>小波多尺度压缩 / 概率分布)
    Abstraction -.- Sub_Abs

    %% 细化内容样式
    style Sub_Rem font-size:12px,text-align:left,fill:#ffffff,stroke-dasharray: 5 5;
    style Sub_Ext font-size:12px,text-align:left,fill:#ffffff,stroke-dasharray: 5 5;
    style Sub_Abs font-size:12px,text-align:left,fill:#ffffff,stroke-dasharray: 5 5;
    
    %% 主流程样式：粗线条，清晰的逻辑流
    linkStyle 0,1,2 stroke-width:3px,fill:none,stroke:green;
```