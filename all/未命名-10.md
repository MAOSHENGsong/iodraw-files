```mermaid
graph TD
    subgraph 一、数据准备与预处理阶段
        A[自建数据集] --> B[按批次随机打乱]
        B --> C[统一缩放至标准分辨率]
        C --> D[引入Mosaic数据增强策略]
    end

    subgraph 二、轻量化主干网络 特征提取
        D -->|预处理后图像| E[输入主干网络]
        E --> F[DSKWConv与C3k2_DSKW: 抑制冗余/提炼特征]
        F --> G[空间金字塔池化: 扩大全局感受野]
        G --> H[C2PSA自注意力机制: 聚焦核心缺陷]
        H --> I(输出三个多尺度特征图)
    end

    subgraph 三、HSFPN颈部网络 特征融合
        I -->|多尺度特征图| J[特征选择模块: 抑制噪声并初筛]
        J --> K[以高导低融合策略: 深层语义与浅层细节交融]
        K --> L(输出高质量融合特征)
    end

    subgraph 四、检测头 预测与定位评估
        L -->|融合特征| M[轻量化无锚框检测头]
        M --> N1[缺陷类别置信度预测]
        M --> N2[边界框坐标回归]
        M --> N3[LQE模块: 量化定位可靠性/生成调整分数]
    end

    subgraph 五、损失计算与反向传播优化
        N1 & N2 --> O[计算预测与真实标注误差]
        O --> P1[分类损失: BCE]
        O --> P2[边界框回归损失: CIOU + DFL]
        P1 & P2 --> Q[各项损失按动态权重线性求和]
        Q --> R[反向传播算法计算梯度]
        R --> S[优化器迭代更新权重]
    end

    subgraph 六、训练循环与模型评估
        S --> T[按批次循环执行: 前向推理与参数迭代]
        T --> U[实时监控验证集: 损失值与mAP]
        U --> V{网络性能是否趋于稳定/触发早停?}
        V -- 否: 继续训练 --> T
        V -- 是: 停止训练 --> W[自动终止训练]
        W --> X((保存评估精度最优的权重文件))
    end
```